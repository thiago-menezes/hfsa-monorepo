{
  "name": "hfsa-monorepo",
  "version": "1.0.0",
  "description": "HFSA Monorepo - Hybrid Feature Scope Architecture",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:main": "turbo run dev --filter=@hfsa/boilerplate",
    "dev:main:with-mock": "turbo run dev:with-mock --filter=@hfsa/boilerplate",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\" && turbo run lint:fix",
    "check:types": "turbo run check-types",
    "check:prettier": "prettier . --check",
    "check:all": "pnpm lint && pnpm check:types && pnpm check:prettier",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:integration": "turbo run test:integration",
    "clean": "turbo run clean",
    "reset": "pnpm clean && rm -rf node_modules && rm -rf **/node_modules && pnpm install",
    "mock": "turbo run mock:api --filter=@hfsa/boilerplate",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=./packages/* && changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.6",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "keywords": [
    "monorepo",
    "turborepo",
    "react",
    "nextjs",
    "design-system",
    "typescript"
  ],
  "author": "HFSA Team",
  "license": "MIT"
}
